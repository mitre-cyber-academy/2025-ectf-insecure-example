name: Pull Request Checks

on:
  pull_request:
    branches: [ main, release ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ./design/
          pip install ./tools/

      - name: Check Python syntax
        run: |
          python -m py_compile $(find ./design -name "*.py")
          python -m py_compile $(find ./tools -name "*.py")

      - name: Build Docker Image
        run: |
          cd decoder
          docker build -t decoder .
      
      # - name: Security scan
      #   run: |
      #     pip install bandit
      #     bandit -r ./design ./tools
          
      # Optional - only include if you have unit tests
      # - name: Run tests
      #   run: |
      #     python -m pytest
